machine:
  java:
    version: openjdk8
  environment:
    ANDROID_HOME: /usr/local/android-sdk-linux
dependencies:
  override:
    - if ! $(grep -q "Pkg.Revision=26.0.1" $ANDROID_HOME/tools/source.properties); then echo y | android update sdk --no-ui --all --filter "tools"; fi
    - if [ ! -e $ANDROID_HOME/build-tools/25.0.2 ]; then echo y | android update sdk --no-ui --all --filter "build-tools-25.0.2"; fi
    - if ! $(grep -q "Pkg.Revision=3" $ANDROID_HOME/platforms/android-25/source.properties); then echo y | android update sdk --no-ui --all --filter "android-25"; fi
    - if ! $(grep -q "Pkg.Revision=3" $ANDROID_HOME/platforms/android-23/source.properties); then echo y | android update sdk --no-ui --all --filter "android-23"; fi
    - if ! $(grep -q "Pkg.Revision=5" $ANDROID_HOME/platforms/android-16/source.properties); then echo y | android update sdk --no-ui --all --filter "android-16"; fi
    - if ! $(grep -q "Pkg.Revision=47.0.0" $ANDROID_HOME/extra/android/m2repository/source.properties); then echo y | android update sdk --no-ui --all --filter "extra-android-m2repository"; fi
    - if ! $(grep -q "Pkg.Revision=47" $ANDROID_HOME/extra/google/m2repository/source.properties); then echo y | android update sdk --no-ui --all --filter "extra-google-m2repository"; fi
  cache_directories:
    - $ANDROID_HOME/tools
    - $ANDROID_HOME/platforms/android-25
    - $ANDROID_HOME/platforms/android-23
    - $ANDROID_HOME/platforms/android-16
    - $ANDROID_HOME/build-tools/25.0.2
    - $ANDROID_HOME/extra/android/m2repository
    - $ANDROID_HOME/extra/google/m2repository
test:
  override:
    - cd android && ./gradlew assembleDebug
    - cp -r ~/$CIRCLE_PROJECT_REPONAME/android/app/build/outputs/apk/* $CIRCLE_ARTIFACTS
      # unit test
    - cd android && ./gradlew testDebugUnitTest 
    - cp -r ~/$CIRCLE_PROJECT_REPONAME/android/app/build/test-results/testDebugUnitTest/* $CIRCLE_TEST_REPORTS
      # test coverage
    - cd android && ./gradlew jacocoTestReport
    - mkdir -p $CIRCLE_TEST_REPORTS/jacoco/
    - cp -r ~/$CIRCLE_PROJECT_REPONAME/android/app/build/reports/jacoco/* $CIRCLE_TEST_REPORTS/jacoco
    - bash <(curl -s https://codecov.io/bash)
deployment:
  master:
    branch: master
    commands:
      - ./gradlew assembleRelease
